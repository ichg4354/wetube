extends layouts/main
include mixins/videoBlock


block content
    .videos
        each video in videoList
            +videoBlock({
                    id:video.id,
                    title:video.title,
                    views:video.views,
                    videofile:video.videofile
                })
    