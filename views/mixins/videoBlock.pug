mixin videoBlock(video = {})
    .videoBlock
        a(href=`videos/${video.id}`)
            video.videoBlock__thumbnail(src=video.fileUrl, controls=true autoplay=true)
            h4.videoBlock__title=video.title
            h6.videoBlock__views=video.views
